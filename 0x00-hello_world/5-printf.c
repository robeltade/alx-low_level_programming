#include <stdio.h>
/*
 * main - A program that prints a line with printf function
 * Return:(0) success
 */
int main(void)
{
printf("with  hI RROBI  i miss u so much proper grammar, but the outcome is a piece of art,");
return (0);
}
